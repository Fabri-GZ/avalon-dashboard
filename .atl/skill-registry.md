# Skill Registry — avalon-dashboard

Generated by sdd-init. Last updated: 2026-06-05.

## User Skills

| Skill | Trigger Context |
|-------|----------------|
| `next-best-practices` | Writing/reviewing Next.js files, RSC boundaries, data fetching, route handlers |
| `vercel-react-best-practices` | React components, Next.js pages, performance, bundle optimization |
| `tailwind-design-system` | Component libraries, design tokens, Tailwind v4 theming |
| `frontend-design` | Building web components, pages, dashboards, UI styling |
| `brainstorming` | Before any creative work — features, components, new functionality |
| `seo-audit` | SEO review, meta tags, crawlability |
| `site-teardown` | Full UI/UX audit of a page or section |
| `branch-pr` | Creating PRs, managing branches |
| `issue-creation` | Filing GitHub issues |
| `requesting-code-review` | Before sending code for review |
| `receiving-code-review` | When reviewing received feedback |
| `shadcn` | shadcn/ui components, `components.json`, registries, composing UI |
| `impeccable` / `ui-ux-pro-max` | UI/UX design, polish, hierarchy, chat/cards layout |
| `supabase` / `supabase-postgres-best-practices` | Supabase DB/Auth/RLS/SSR, migrations, Postgres queries |
| `react-doctor` | Finishing a feature/bugfix, before commit; `pnpm doctor` |
| `claude-api` | LLM integration — intent parsing, structured output, tool use (agente IA) |
| `n8n-*` (workflow-patterns, code-javascript, expression-syntax, node-configuration, mcp-tools-expert, validation-expert) | Building/editing n8n workflows (agent layer — later phase) |

## Project Conventions

| Source | Path |
|--------|------|
| Global CLAUDE.md | `~/.claude/CLAUDE.md` |
| Obsidian project note | `Brain/02-Avalon/Proyectos/avalon-dashboard.md` |
| Obsidian CRM note | `Brain/02-Avalon/Proyectos/avalon-dashboard-crm.md` |

## Compact Rules (inject into sub-agents)

### Next.js / React
- App Router only. Server Components by default; `"use client"` only for interactivity/hooks.
- Data fetching in Server Components or Route Handlers — never fetch Supabase from client directly.
- `createClient()` from `@/app/utils/supabase/server` for server-side; `createClient()` from `@/app/utils/supabase/client` for client-side.
- Middleware (`middleware.ts`) handles auth + role-based redirects at the edge.
- `DashboardShell` wraps all dashboard pages: `DashboardDataContext` + `DashboardUIContext`.

### Permissions / Roles
- Roles: `admin_global`, `client_user`, `cm`, `pm`, `comercial`.
- `section_permissions` table in Supabase controls what each role can see.
- `defaultRouteForRole('client_user')` = `/dashboard/chatbot/crm`.
- `requiredSectionFor(pathname)` maps routes to section keys — add new routes here when adding sections.

### Supabase
- Dashboard project ID: `ntopxgotcuannmtmfdbh`.
- RLS on all tables. `admin_global` = full access; `client_user` = SELECT own `client_id`.
- n8n writes with `service_role` (bypasses RLS).
- Two Supabase projects: `fabrizio.avalon3` (bot memory) and `avalon-agency-dashboard` (dashboard).

### CRM (Grupo Norte)
- `gn_leads` — one row per lead, PK `(channel, session_id)`.
- `gn_messages` — one row per turn, plain text, FK → gn_leads.
- Grupo Norte client_id: `b7859a5a-d306-488a-ba3d-6733ae8430ad`.
- Stages: `nuevo`, `conversando`, `derivado`, `cerrado`, `sin_respuesta`.

### Styling
- Tailwind v4. Tokens in `globals.css` via `@theme {}`. No `tailwind.config.js`.
- shadcn/ui components. Framer Motion for animations. Recharts for charts.
- Color system: `primary`, `secondary`, `accent`, `muted`, `border`, `foreground`, `background`.

### Testing (NO test runner)
- No hay vitest/jest/playwright. Strict TDD **disabled**. Verificación = `pnpm lint` + `pnpm exec tsc --noEmit` + `pnpm doctor` (react-doctor) + revisión visual.

### Commits
- Conventional commits only. No AI attribution. Never `--no-verify`.
